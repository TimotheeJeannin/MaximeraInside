[project]
name = "maximera-inside"
version = "0.1.0"
description = "Tooling for the MAXIMERA drawer 3D model"
requires-python = ">=3.10"
dependencies = ["trimesh>=4.4", "numpy>=1.26", "pillow>=10.0"]

[tool.uv]
package = false
